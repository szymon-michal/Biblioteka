import React from "react";
import { Alert, Box } from "@mui/material";

export default function AdminStats() {
    return (
        <Box>
            <Alert severity="info">
                Statystyki są na Dashboardzie ADMIN (główna strona). Ten widok możesz rozbudować później.
            </Alert>
        </Box>
    );
}
